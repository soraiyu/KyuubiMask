# ビルドと実機テストガイド 📱

このドキュメントは、KyuubiMask を実機でビルド・テストするための手順をまとめています。

## 前提条件

### 必要なツール

1. **Android Studio** (推奨: 最新安定版)
   - Download: https://developer.android.com/studio

2. **JDK 17**
   - Android Studio に同梱されている JDK でOK
   - または OpenJDK 17 をインストール

3. **Android デバイスまたはエミュレーター**
   - Android 8.0 (API 26) 以上
   - 開発者オプションが有効

### dev container での Tailscale 利用（任意）

systemd なしの dev container で Tailscale を使う場合は、次の補助スクリプトを使用してください。

```bash
# Tailscale の開始/再接続
./scripts/tailscale-up.sh

# Tailscale から切断
./scripts/tailscale-down.sh

# ローカル tailscaled デーモン停止
./scripts/tailscaled-stop.sh
```

## ビルド手順

### 1. コマンドラインからビルド

```bash
# リポジトリのルートディレクトリに移動
cd KyuubiMask

# Debug APK をビルド
./gradlew assembleDebug

# APK の場所
# app/build/outputs/apk/debug/app-debug.apk
```

**ビルドに失敗する場合**:
```bash
# キャッシュをクリア
./gradlew clean

# 再度ビルド
./gradlew assembleDebug
```

### 2. Android Studio からビルド

1. Android Studio を起動
2. "Open" から KyuubiMask フォルダを開く
3. Gradle sync が自動的に開始
4. メニューから `Build > Build Bundle(s) / APK(s) > Build APK(s)`
5. ビルド完了後、通知から APK の場所を確認

## インストール手順

### 方法1: ADB 経由でインストール

```bash
# デバイスが接続されているか確認
adb devices

# APK をインストール
adb install app/build/outputs/apk/debug/app-debug.apk

# 既にインストール済みの場合は -r オプションで上書き
adb install -r app/build/outputs/apk/debug/app-debug.apk
```

### 方法2: ファイル転送でインストール

1. APK ファイルをデバイスに転送（USB、メール、クラウドなど）
2. デバイスで APK ファイルをタップ
3. "提供元不明のアプリのインストール" を許可
4. インストール実行

### 方法3: Android Studio から直接実行

1. USB デバッグを有効にしたデバイスを接続
2. Android Studio で実行ボタン（緑の▶）をクリック
3. デバイスを選択
4. アプリが自動的にビルド・インストール・起動

## 動作テスト手順

### 初回セットアップテスト

1. **アプリを起動**
   - ホーム画面に KyuubiMask アイコンが表示される
   - アプリを開く

2. **通知アクセス権限の付与**
   - "Grant Notification Access" ボタンをタップ
   - 設定画面が開く
   - KyuubiMask を探してタップ
   - 「通知へのアクセスを許可」をオンにする
   - 警告ダイアログが表示されたら「許可」

3. **ステータス確認**
   - アプリに戻る
   - ステータスが「🦊 Active & Protecting」になっていることを確認

### 通知マスキングテスト

#### テスト1: WhatsApp の通知をマスクする

**前提**: WhatsApp がインストールされている

1. KyuubiMask で "WhatsApp" チップが選択されていることを確認
2. WhatsApp で誰かにメッセージを送ってもらう（または自分に送る）
3. 通知バーを確認:
   - ✅ 正常: "WhatsApp" / "New notification" と表示される
   - ❌ 異常: メッセージ内容が見える

#### テスト2: マスキングの無効化

1. KyuubiMask で "Enable Notification Masking" スイッチをオフにする
2. WhatsApp で再度メッセージを受信
3. 通知バーを確認:
   - ✅ 正常: メッセージ内容がそのまま表示される
   - ❌ 異常: マスクされている

#### テスト3: アプリごとの設定

1. KyuubiMask で "Telegram" チップをオンにする
2. WhatsApp と Telegram の両方から通知を受信
3. 確認:
   - WhatsApp: マスクされる ✅
   - Telegram: マスクされる ✅
4. WhatsApp チップをオフにする
5. 再度両方から通知を受信
6. 確認:
   - WhatsApp: マスクされない ✅
   - Telegram: マスクされる ✅

### デバッグログの確認（Debug ビルドのみ）

Debug ビルドの場合、アプリ画面下部にリアルタイムログが表示されます：

```
[14:23:45] App started. Waiting for notifications...
[14:24:10] ✅ Service connected!
[14:24:32] 🦊 MASKED: com.whatsapp
[14:24:32] ✅ Masked notification posted for: WhatsApp
```

このログで以下を確認:
- サービスが接続されているか
- 通知が正しく検出されているか
- マスキングが実行されているか

## トラブルシューティング

### 問題: ビルドが失敗する

**解決策**:
```bash
# キャッシュクリア
./gradlew clean

# Gradle wrapper を更新
./gradlew wrapper --gradle-version 8.10

# 再ビルド
./gradlew assembleDebug
```

### 問題: 「通知へのアクセス」設定画面に KyuubiMask が表示されない

**原因**: アプリが正しくインストールされていない

**解決策**:
1. アプリをアンインストール
2. 再度インストール
3. デバイスを再起動

### 問題: 通知がマスクされない

**チェックリスト**:
- [ ] 通知アクセス権限が付与されているか？
- [ ] サービスが「Active & Protecting」になっているか？
- [ ] マスキングスイッチがオンになっているか？
- [ ] 対象アプリのチップが選択されているか？
- [ ] デバッグログに "Service connected" が表示されているか？

**解決策**:
1. アプリを再起動
2. 通知アクセス権限を一度オフにしてから再度オンにする
3. デバイスを再起動

### 問題: アプリがクラッシュする

**情報収集**:
```bash
# ログを確認
adb logcat -s KyuubiMask:V AndroidRuntime:E

# または全ログ
adb logcat > crash.log
```

**報告**: GitHub Issues にログを添付して報告してください

## パフォーマンステスト

### バッテリー消費テスト

1. デバイスの設定 > バッテリー > バッテリー使用状況を開く
2. 1日使用後、KyuubiMask の消費量を確認
3. 期待値: 1%未満（ほぼゼロ）

### メモリ使用量テスト

```bash
# メモリ使用量を確認
adb shell dumpsys meminfo com.kyuubimask
```

期待値: 20-30 MB 程度

## セキュリティ検証

### 1. インターネットアクセスの確認

```bash
# アプリの権限を確認
adb shell dumpsys package com.kyuubimask | grep permission

# INTERNET 権限がないことを確認
# 出力に "android.permission.INTERNET" が含まれていないこと
```

### 2. ネットワークトラフィックの監視

1. ネットワーク監視アプリ（例: NetGuard）をインストール
2. KyuubiMask を使用しながら監視
3. 確認: ネットワーク通信が発生していないこと ✅

## リリースビルドのテスト

### 署名付き APK の作成

```bash
# キーストアの作成（初回のみ）
keytool -genkey -v -keystore kyuubimask.keystore -alias kyuubimask -keyalg RSA -keysize 2048 -validity 10000

# リリースビルド（署名が必要）
./gradlew assembleRelease
```

**注意**: リリースビルドでは:
- デバッグログが表示されない（BuildConfig.DEBUG = false）
- コードが最適化される（ProGuard/R8）
- APK サイズが小さくなる

## CI/CD での自動ビルド（将来）

GitHub Actions での自動ビルド例:

```yaml
name: Build APK
on: [push, pull_request]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-java@v3
        with:
          java-version: '17'
      - name: Build Debug APK
        run: ./gradlew assembleDebug
      - name: Upload APK
        uses: actions/upload-artifact@v3
        with:
          name: app-debug
          path: app/build/outputs/apk/debug/app-debug.apk
```

## テスト完了チェックリスト

実機テストが完了したら、以下を確認:

- [ ] ビルドが成功する
- [ ] アプリが起動する
- [ ] 通知アクセス権限を付与できる
- [ ] 通知が正しくマスクされる
- [ ] マスキングの有効/無効が切り替えられる
- [ ] アプリごとの選択が機能する
- [ ] バッテリー消費が少ない
- [ ] メモリ使用量が適切
- [ ] ネットワーク通信が発生しない
- [ ] クラッシュしない

## フィードバック

テスト結果は GitHub Issues で報告してください：
https://github.com/soraiyu/KyuubiMask/issues

**報告内容**:
- デバイス情報（機種、Android バージョン）
- テスト結果（成功/失敗）
- 問題があれば詳細とログ
- 改善提案

---

**Happy Testing! 🦊**
